import { a } from './utils';

function fancyColor(color: string | object, alpha: number) {
  color = color + '';
  alpha = alpha + 1;
  let c: number = 1;
  const b: number = a + c;
  return b;
}

export default fancyColor;
